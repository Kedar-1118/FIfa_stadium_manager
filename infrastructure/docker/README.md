# Docker configurations

## Why it exists
Container orchestrations for developer sandboxes.

## Responsibility
Simulates DBs, Kafka, Redis, and services locally.

## What classes/files belong here
- docker-compose.yml (standard service stack setup)
- docker-compose.override.yml (custom developer variables config)
